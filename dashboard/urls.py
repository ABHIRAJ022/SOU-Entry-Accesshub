from django.urls import path
from . import views
app_name = 'dashboard'
urlpatterns = [path('', views.home, name='home'), path('security/', views.security_dashboard, name='security'), path('map/', views.campus_map, name='campus_map'), path('users/<int:user_id>/approval/', views.approve_user, name='approve_user'), path('students/lookup/', views.student_lookup, name='student_lookup'), path('guest-requests/', views.guest_requests, name='guest_requests'), path('guest-requests/<int:request_id>/', views.guest_request_detail, name='guest_request_detail'), path('guest-requests/<int:request_id>/approve/', views.approve_guest_request, name='approve_guest_request'), path('guest-requests/<int:request_id>/reject/', views.reject_guest_request, name='reject_guest_request'), path('tokens/manage/', views.token_management, name='token_management'), path('tokens/history/<int:user_id>/', views.token_history, name='token_history'), path('tokens/<uuid:token_id>/cancel/', views.cancel_token, name='cancel_token'), path('tokens/generate/', views.issue_token, name='issue_token'), path('tokens/regenerate/', views.regenerate_token, name='regenerate_token'), path('tokens/<uuid:token_id>/status/', views.token_status, name='token_status'), path('tokens/<uuid:token_id>/pdf/', views.token_pdf, name='token_pdf')]
